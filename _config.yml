# The template is from Minima
#   https://github.com/jekyll/minima
# I deleted a lot of unnecessary stuff from the template, so read the docs if
# there's a feature to add back into this

title: Sam Stromwall's Blog
author:
  name: Sam Stromwall
  # email: your-email@domain.com

# description: >
#   Blog by Sam Stromwall

theme: minima

plugins:
  - jekyll-feed
  - jekyll-seo-tag

sass:
  quiet_deps: true
  silence_deprecations: [import]

minima:
  #   Available skins:
  #     * classic            Default, light color scheme.
  #     * dark               Dark variant of the classic skin.
  #     * auto               Adaptive skin based on the default classic and dark skins.
  #     * solarized-light    Light variant of solarized color scheme.
  #     * solarized-dark     Dark variant of solarized color scheme.
  #     * solarized          Adaptive skin for solarized color scheme skins.
  skin: auto

  # Specific pages for site navigation.
  #   If you wish to link only specific pages in the site-navigation, use this and list
  #   the `path` property (as represented via Liquid) of the pages in the order they
  #   should be rendered.
  nav_pages:
    - about.md

  # Set to `true` to show excerpts on the homepage.
  show_excerpts: true

  # Minima date format.
  #   The default value is "%b %d, %Y" (e.g. Nov 14, 2023).
  #   Refer to https://shopify.github.io/liquid/filters/date/ for valid values.
  # date_format: "%b-%d-%Y"

  social_links:
    - title: Github
      icon: github
      url: "https://github.com/sstromw"
    - title: LinkedIn
      icon: linkedin
      url: "https://www.linkedin.com/in/samuel-stromwall-054536a9"
    # - title: Jekyll at X (formerly Twitter)
    #   icon: x-twitter
    #   url: "https://x.com/jekyllrb"
